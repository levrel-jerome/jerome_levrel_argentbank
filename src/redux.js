// deja le formulaire faire un consolelog pour recup les données a la valid
// crée le store qui conservera l'utilisateur
// appel de l'api avec ces données avec le fetch post
// retour appel conserver le token dans le store + redirect vers page profil
// faire la page profil (appel api nom prenom)
// afficher logout si deja co
// modifier nom prenom
// plus possible d'aller sur la page co une fois co
// ueffeect pour ne plus aller sur le sign in une fois log

//appuis sur le bouton le fait disparaitre
//fait apparaitre la div change
//recuperation des donnée rentrées dans les input
//si appuis sur bouton save change nom prenom bdd
//si cancel fait disparaitre div change et reapparaitre bouton edit
//utiliser axios put pour changer les données

//post/user/transactions
//Authorization Bearer jwt
//Endpoint: Affichage des transactions
//itemId = id de la transaction concernée
//error: invalid transactions

//{!userService.isLogged() "= if condition" ?<Route path="/sign" element={<Sign />}></Route>: = "else"
//<Route path="/user" element={<User />}></Route>}
//ex dans router islogged
